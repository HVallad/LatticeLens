code: API-03
layer: HOW
type: API Specification
fact: 'The MCP server exposes a read-only reconcile tool that runs bidirectional codebase
  reconciliation and returns a summary dictionary. The tool accepts optional parameters:
  path (codebase directory), include/exclude (glob patterns). It returns a dict with
  keys confirmed, stale, violated, untracked, orphaned (integer counts) and coverage_pct
  (float). The tool is always available (no write permissions needed) and delegates
  to tool_reconcile() in mcp/tools.py, which calls the reconcile service with the
  active LatticeStore instance.'
tags:
- api
- mcp
- read-only
- reconciliation
status: Active
confidence: Confirmed
version: 3
refs:
- code: DES-11
  rel: implements
- code: DES-12
  rel: implements
- code: SP-10
  rel: depends_on
- code: API-01
  rel: depends_on
superseded_by:
owner: architecture-team
review_by: '2026-09-01'
created_at: '2026-03-05T23:56:08.515575'
updated_at: '2026-03-07T11:23:15.852319'
projects: []
