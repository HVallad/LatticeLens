code: API-01
layer: HOW
type: API Specification
fact: >-
  The LatticeStore protocol defines the storage abstraction: get, list_facts,
  create, update, deprecate, exists, all_codes, stats. All CLI commands and
  future MCP tools program against this protocol. New backends must implement
  all methods.
tags: [protocol, storage-abstraction, api, extensibility]
status: Active
confidence: Confirmed
owner: architecture-team
refs: [DES-02, ADR-03]
