code: RUN-01
layer: HOW
type: Runbook Procedure
fact: >-
  Schema migrations are handled by lattice upgrade. Migrations are defined as
  an ordered list of (version, description, function) tuples. The runner reads
  the current version from config.yaml, applies only needed migrations in
  order, and stamps the new version. Always idempotent and safe to re-run.
tags: [migration, upgrade, versioning, operations]
status: Active
confidence: Confirmed
owner: architecture-team
refs: [DG-01, PRD-01]
